# Deepstream.io + Redis Cluster

Just to show that Deepstream + redis cluster doesn't work as intended. Deepstream keeps reconnecting to the primary redis.

Just clone and run `docker-compose up` to test.
